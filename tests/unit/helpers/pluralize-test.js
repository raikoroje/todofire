import {
  pluralize
} from 'todofire/helpers/pluralize';

module('PluralizeHelper');

// Replace this with your real tests.
test('it works', function() {
  var result = pluralize(42);
  ok(result);
});
